# 習慣記録アプリ 仕様書

DotHabit（iOSアプリ）を自分用に置き換える。作者本人だけが使う。公開・配布はしない。

---

## 1. 技術的な制約

- **PWA**（ホーム画面に追加して使う）。ネイティブアプリにはしない
- **素のHTML / CSS / JavaScript のみ**。React、Vue、Tailwind等のフレームワークは使わない
- **ビルド工程を作らない**。npm、バンドラ、トランスパイラは使わない
- 外部CDNからのライブラリ読み込みも行わない（オフラインで動かなくなるため）
- アプリ本体は `index.html` 1ファイルに収める（CSS・JSともにその中）
- **オフラインで起動できること**。Service Worker でキャッシュする
- 置き場所は **GitHub Pages**

### ファイル構成

```
index.html              アプリ本体（HTML + CSS + JS すべて）
sw.js                   Service Worker
manifest.webmanifest    アプリ名・アイコン・起動設定
icon-192.png
icon-512.png
icon-maskable-512.png
```

リポジトリ直下に置き、Settings → Pages → Source を `main / (root)` にして公開する。

`index.html` を変更したら `sw.js` の `CACHE` 名（`habit-v1` → `habit-v2`）を必ず上げること。上げないと古い版が表示され続ける。

---

## 2. データ

### 保存先

`localStorage`、キーは `habit.v1`。読み書きは必ず try/catch で囲む（プライベートブラウズ等で例外が出る）。

### 形

```json
{
  "version": 1,
  "habits": [
    {
      "id": "h1",
      "name": "顔トレ",
      "color": "#3FC57D",
      "startDate": "2024-03-20",
      "order": 0,
      "archived": false,
      "completedDate": null,
      "records": {
        "2026-09-04": "",
        "2026-09-03": "ベンチ 60kg×8"
      }
    }
  ]
}
```

`habits` と並べて、最後にバックアップを書き出した日を `lastExportedAt`（`YYYY-MM-DD`、未書き出しは `null`）で持つ。

- `records` は日付をキーにしたオブジェクト。値はメモ文字列で、メモ無しは空文字
- 日付は必ず **ローカル時刻**で `YYYY-MM-DD` を作る。`toISOString()` は UTC になるため使用禁止
- `completedDate` は達成済みにした習慣のみ。稼働中は `null`
- メモは全習慣で書ける。習慣ごとのメモ設定は持たない

### 初期データの扱い

**移行データ（`dothabit-migration.json`）はリポジトリに含めない。** 作者の習慣名と2,366日ぶんの実施日が入っており、GitHub Pages を無料で使うにはリポジトリを Public にする必要があるため。`.gitignore` に入れてある。

- アプリは**空の状態で起動する**（習慣ゼロ、「習慣を追加」だけが見える画面）
- 移行データは、完成後に 8節のバックアップ「読み込み」欄へ**一度だけ貼り付けて**取り込む
- 開発中の動作確認には、手元の `dothabit-migration.json` を使ってよい。ただしコミットしないこと

---

## 3. 計算ルール

すべて日付の文字列比較ではなく、日付オブジェクトで計算すること。

| 値 | 定義 |
|---|---|
| 累計完了日数 | `records` の件数 |
| 通算日数（◯日目） | `(今日 - startDate).days + 1`。開始日が1日目 |
| 連続日数（記録一覧のバッジ） | その日で終わる連続記録の長さ。前日に記録が無ければ 1 |
| 現在の連続日数 | 今日に記録があれば今日から遡って数える。無ければ**前日から**遡る（朝に 0 と出さないため） |
| 達成日 | 達成済みにした時点で、記録のある最終日を `completedDate` に入れる |

検算用：2026-09-04 時点で 読書 = 928日目、芝浦 ゼミ課題 = 838日目、顔トレ = 899日目。

---

## 4. ホーム画面

稼働中の習慣（`archived: false`）だけを `order` 順に、**2列のカード**で並べる。

### カード1枚の構成

```
┌──────────────┐
│ ■ □ □ □ ■ □ □ │   ← 35マス
│ □ ■ ■ □ ■ ■ ■ │
│ ■ ■ ■ ■ ■ ■ ■ │
│ □ ■ ■ ■ ■ ■ ■ │
│ ■ □ ■ ■ ■ □ □ │
│    ◍ 544      │   ← 炎アイコン ＋ 累計完了日数
│    顔トレ      │   ← 習慣名（白）
└──────────────┘
```

### 炎アイコンの色

**今日の記録の有無で色が変わる。**

- 今日すでに記録がある … その習慣の `color`
- まだ記録が無い … グレー `#6B7076`

絵文字（🔥）は使わない。インライン SVG かCSSで描いた単色のアイコンにして、色を差し替えられるようにする。絵文字だと色を変えられず、オレンジが配色から浮く。

これにより「今日やったか」が、**破線のマスと炎の色の2か所**で分かる。カードのどこを見ていても目に入る。

習慣名は**白**（`#FFFFFF`）。補助テキストの薄いグレーにしない。

### マスの並び（重要）

**これはカレンダーではない。** 曜日は揃えない。

- 7列 × 5行 = 35マス
- **左上が今日**。右へ進むごとに1日ずつ過去へ、行末まで行ったら次の行の左端へ続く
- つまり `i` 番目のマス（0始まり、左上から右下へ）は `今日 - i 日`
- 右下のマスが 34日前

### マスの見た目

- 角丸の正方形（丸ではない）
- 記録あり … その習慣の `color`
- 記録なし … 暗いグレー `#30343A`
- **今日のマスに記録が無いとき** … 塗りつぶさず、`color` の**破線の枠**にする

---

## 5. 記録する操作

ホーム画面のマスを直接タップして記録する。詳細画面に入る必要はない。**全習慣で同じ挙動**にする。習慣ごとのメモ設定は作らない。

### タップ

マスをタップ → **その場で記録が付く／外れる**。画面遷移は起こさない。

記録が付いたときだけ、マスが少し膨らんで戻る程度のアニメーションを付ける。`prefers-reduced-motion` を尊重すること。

### 「メモを追加」の帯

記録が**付いた**直後、画面下部に「メモを追加」の帯を約4秒出す。

- 押すとメモ入力欄が開く。日付の見出し（例: `2026年9月4日（金）`）、複数行のテキスト欄、「完了」ボタン
- 押さなければ静かに消える。記録はそのまま残る
- 記録を**外した**ときは帯を出さない
- 帯が出ている間に別のマスをタップしたら、帯の対象を新しい記録に差し替える

これにより、メモを書かない日は**1タップで終わり**、書きたい日だけ帯を押せばよい。

### すでに記録がある今日のマスをタップしたとき

記録が外れる（トグル）。**その記録にメモが入っている場合は、メモも消える旨を確認してから消す。**

書いたメモを後から読む・直すのは詳細画面（6節）から行う。

### タップできるのは今日のマスだけ

**過去のマスはタップしても記録されない。** 35マスは小さく、スクロール中の誤タップで過去の記録を汚してしまう危険があるため。

過去のマスをタップしたときは「過去の記録は履歴から追加できます」と2秒ほど表示し、**データは一切変更しない**。未来の日はマス自体を描かない。

過去の日に記録を付けるのは、詳細画面（6節）の「＋」から行う。

---

## 6. 詳細画面（履歴）

ホーム画面の習慣名をタップして開く。

### 構成

- ヘッダ：戻る、習慣名、設定ボタン
- 通算日数（例: `929日目`）を右上に
- ステータス行：今日まだ記録が無ければ「今日も完了させましょう！」、記録済みなら「◯日連続達成中」
- 記録の一覧（**新しい順**）。縦の線でつないだタイムライン形式にする

各行の内容：

```
┌──┐
│ 3│  2026/09/04   928日目
└──┘  胸　肩　腕                    ⋯
  │
┌──┐
│ 1│  2026/08/31   924日目
└──┘  全身                          ⋯
```

- 1件ごとを**角丸のカード**で囲む（背景 `#48484A`）
- 左のバッジ＝**その日が連続何日目か**（習慣の色の角丸四角に数字）。カードの中に置く
- 日付と通算日数を1行目、メモを2行目
- 右端の「⋯」から、その記録の編集・削除

### 縦線は「連続」を意味する

**線は、連続した日の記録どうしの間にだけ引く。**

- 08/23 と 08/22 のように日付が連続していれば、線でつなぐ
- 09/04 と 08/31 のように間が空いていれば、**線を引かない**（カードの間が空くだけ）

いまのように全部つないでしまうと、9日連続した区間と4日空いた区間が同じに見え、バッジの数字を読まないと区別できない。線に意味を持たせる。

一覧が長くなるので、まず直近100件を出して「もっと見る」で追加する形でよい。

### 年別カレンダー（リストと切り替え）

移行した2,368日のうち、ホーム画面で見えるのは直近35日、リストは1件ずつ。**2024年・2025年の記録を一望する手段が無い**ので、それを補う画面。

- 詳細画面の下部にバーを置き、**リスト表示 ☰ ／「＋」／ カレンダー表示 ▦** を並べる（DotHabit と同じ配置）
- カレンダー表示の上部に年のタブ。**記録がある年だけ**出す（例: 2026 / 2025 / 2024）
- 12か月ぶんのミニカレンダーを縦に並べる。日〜土の7列
- その習慣の記録がある日を、習慣の色で塗る
- 年ごとの**完了日数を1行だけ**添える。最高継続日数・完了率・レベル称号は作らない（数字を並べる画面にはしない）
- **読み取り専用**。記録の追加・編集は「＋」から行う

### 記録を追加（過去の日はここから）

**「＋」ボタンは下部バーの中央に置く。** 一覧の末尾に置くと、記録が214件ある習慣では末尾までスクロールしないと押せない。過去の記録を足すのが主用途なので、常に届く位置に置く。

押すと入力画面が開く。

- 上部に日付を出す：`‹　2026年9月5日（土）　›`
  - 矢印で1日ずつ前後に動かせる
  - **日付そのものをタップすると、アプリ内の月カレンダーが開く**
  - 未来には進めない

**日付ピッカーに `<input type="date">` と `showPicker()` は使わないこと。** iOS Safari ではどちらも開かない（WebKit の既知の不具合、2025年10月時点で未解決）。実機で確認済み。

月カレンダーの要件：

- 日〜土の7列。前月・次月の矢印付き
- **その習慣の記録がある日を、習慣の色で塗る**（どの日が抜けているか見えるように）
- 今日より先の日は薄く表示して選べない
- 日付を選ぶとカレンダーが閉じ、その日付が選ばれた状態になる
- カレンダーを開くとシートが縦に伸びるので、小さい端末でも「完了」に届くようスクロールできること

日付の下は、メモ欄（複数行、任意）と「完了」ボタン。

選んだ日にすでに記録がある場合は、その日のメモを読み込んで表示し、ボタンを「保存」に変える。あわせて「記録を取り消す」も出す。

---

## 7. 習慣の管理

### 追加

**入口**：ホーム画面のカード一覧の**最後に「＋」のカードを並べる**（点線枠）。習慣が0件のときは、それだけが画面に出る。

入力するのは3つだけ。

- 名前
- 色（下の7色から選ぶ）
- 開始日（既定は今日。過去日も選べる）

目標の文言と通知は作らない（10節）。

色は DotHabit と同じ7色を用意する：
`#3FC57D` 緑 / `#5AAFE2` 青 / `#E5484D` 赤 / `#EC5E9F` ピンク / `#EDC24B` 黄 / `#0596A6` ティール / `#8B84D7` 紫

### 変更

名前、色、**開始日**。入口は詳細画面の右上の ⚙。

開始日も変更できるようにする。追加のときに打ち間違えると通算日数が全部ずれるうえ、直す手段が無くなるため。

### 並び替え

**設定画面（⚙）に、並び替え専用の一覧を用意する。** 稼働中の習慣を縦に並べ、その場で上下ボタンで動かす。

習慣ごとの設定シートの中に上下ボタンを置くと、移動した結果を見るのにいちいちホーム画面へ戻ることになる。6件の順番を入れ替えるだけで何往復もすることになるので、全体が見える1画面で完結させる。

### 達成済みにする

ホーム画面から外し、`archived: true`、`completedDate` に最終記録日を入れる。

**達成済み一覧**の画面を1つ用意して、そこから見られるようにする。記録は消さない。稼働中に戻せること。

### 削除

確認ダイアログを出してから消す。

---

## 8. バックアップ

設定画面に「データのバックアップ」を置く。

- **書き出し** … 全データの JSON をテキスト欄に表示する。`<a download>` によるダウンロードは使わない（環境によっては動かないため）。ファイルとして持ち出すのは下の「共有」から行う
- 書き出しまたは共有をしたら、`lastExportedAt` に今日の日付を入れる

### 共有

書き出しの隣に「共有」ボタンを置き、端末の共有シートを開く。**優先順位は3段**。

1. `navigator.canShare({ files })` が使えるなら、**JSON をファイルとして**共有する。ファイル名は `habit-backup-2026-09-06.json` のように日付入り、type は `application/json`
2. ファイル共有が使えなければ `navigator.share({ text })`
3. どちらも使えなければ、テキスト欄に出して全選択する（何も壊さない）

**テキスト共有を第一候補にしないこと。** 44KB のテキストを `navigator.share({ text })` で渡すと、iOS のメモが受け取れずに一瞬開いて即座に閉じる。実機で確認済み。

利用者がファイル共有を取り消した場合、**テキスト共有に落とさない**。上の不具合を踏み直すため。

### 読み込み

入口を2つ用意する。どちらも**まったく同じ処理**を通す。

- テキスト欄に貼り付けた JSON
- 「ファイルから読み込む」ボタン（`input type="file"`）で選んだ JSON ファイル

貼り付けた内容で全データを置き換える。壊れた JSON は「読み込めませんでした」と出して何も変更しない。

書き出しがファイルになった以上、読み込みもファイルを受けられないと片道になる。

読み込みは**2つの形式を受け付ける**こと。

1. アプリ自身が書き出した形式（`records` が日付をキーにしたオブジェクト）
2. 移行データの形式（`records` が日付文字列の配列。メモは無い）

2 の場合は `{"2026-09-04": "", ...}` に変換して取り込む。移行データにはこのアプリに無い項目が入っていても無視してよい。

### バックアップを促す

記録はこの端末の中にしかなく、実際に一度消えている。手順が面倒だと続かないので、共有ボタンと合わせて**思い出させる仕組み**を作る。

- `lastExportedAt` から **30日以上**経っていたら、ホーム画面の上部に小さい帯を出す
- 文言は「バックアップから◯日経っています」。押すと共有シートが開く
- 「あとで」で閉じられる。閉じたら**7日間は出さない**
- 一度も書き出していない場合（`lastExportedAt` が `null`）も同じ帯を出す
- 毎日出るものにはしない。うるさい通知は無視されるようになる

---

## 9. 見た目

DotHabit と同じ路線にする。

- 背景 `#1C1C1E` / カード `#48484A` / マスの空 `#30343A`
- 文字 白、補助テキストは `#A0A4A8`
- 日本語フォントは端末標準（`Hiragino Sans`, `Yu Gothic`, `sans-serif`）。Webフォントは読み込まない
- 上下のセーフエリアを `env(safe-area-inset-*)` で確保する
- ダークのみ。ライトテーマは作らない

---

## 10. 作らないもの

通知、AI機能、友達機能、会員登録、クラウド同期、パスワードロック、レベル称号、目標の文言、写真の添付、数値入力、曜日指定。

### 後回し（最初は作らない）

最高継続日数、完了率、レベル称号、100日チャレンジ。

これらは作者が「たまにしか見ない」と判断した機能。毎日使う部分を先に完成させ、実際に使ってから必要になれば足す。

年別カレンダーは当初ここに入れていたが、**移行した2,368日を一望する唯一の手段**であることが分かったため、6節に移して作ることにした。統計としてではなく、過去を見る手段として。

---

## 11. 合格ライン

以下がすべて満たされたら完成とする。

1. 開くと稼働中6つが2列×3行で並ぶ
2. どの習慣でも、マスをタップすると**画面が変わらないまま**今日が埋まる
3. 記録した直後に「メモを追加」の帯が出る。押せばメモが書け、押さなければ数秒で消えて記録だけ残る
4. ホーム画面で記録できるのは**今日のマスだけ**。過去のマスをタップしても記録は変わらず、案内が出るだけ
5. 詳細画面の「＋」から日付を選んで、過去の日に記録を追加できる
6. `dothabit-migration.json` を「読み込み」に貼り付けると8習慣が取り込まれ、累計完了日数が DotHabit と一致する
   （顔トレ 544 / 筋トレ 547 / ピアノ 64 / 読書 214 / 振り返り 151 / 芝浦 ゼミ課題 391 / 就活 78 / 武相庵に行く！ 377）
7. 2026-09-04 時点で 読書が `928日目`、芝浦 ゼミ課題が `838日目` と表示される
8. 達成済みにした習慣がホームから消え、達成済み一覧に残る
9. **機内モードで起動できる**
10. ホーム画面に追加すると独自アイコンが出て、起動時にブラウザのUIが出ない
11. 書き出した JSON を別のブラウザで読み込むと、同じ状態が再現される

---

## 12. 実装の順番

1. ~~データ構造とホーム画面の描画~~ **完了**
2. ~~マスのタップによる記録と「メモを追加」の帯~~ **完了**
3. ~~バックアップの書き出しと読み込み~~ **完了**（順番を繰り上げた）
4. ~~詳細画面（履歴）と「＋」からの過去の記録の追加~~ **完了**
5. ~~ホーム画面のタップを今日のマスだけに制限~~ **完了**
6. 実物と見比べての手直し（4節の炎アイコンと習慣名、6節の縦線・カード・「＋」の位置）← **次はここ**
7. 習慣の追加・変更・達成済み・削除
8. ~~PWA 化~~ **完了**
9. バックアップの共有ボタンと、書き出しを促す帯（8節）
10. 年別カレンダー（6節）

各段階で `index.html` をブラウザで開いて目視確認すること。段階ごとに、11節の合格ラインのうち関係する項目を確かめてから次へ進む。
